--- TABLAS ---
analisis_chagas, analisis_glicemia, analisis_grupo_sanguineo, analisis_laboratorio, analisis_parasitologia, analisis_vih, clientes, grupos_sanguineos, historial_medico, roles, tipos_trabajo, usuarios

--- TABLA: analisis_chagas ---
- id_chagas (integer, NOT NULL)
- fase_sospechada (character varying, NULL)
- prueba_elisa (character varying, NULL)
- resultado_elisa (character varying, NULL)
- indice_elisa (numeric, NULL)
- prueba_hat (character varying, NULL)
- resultado_hat (character varying, NULL)
- prueba_ifi (character varying, NULL)
- resultado_ifi (character varying, NULL)
- titulo_ifi (character varying, NULL)
- gota_gruesa (boolean, NULL)
- resultado_gota_gruesa (character varying, NULL)
- estudio_xenodiagnostico (boolean, NULL)
- resultado_final (character varying, NOT NULL)
- control_positivo (boolean, NULL)
- control_negativo (boolean, NULL)
- observaciones (text, NULL)
- id_responsable (integer, NOT NULL)
- fecha_analisis (timestamp with time zone, NOT NULL)

--- TABLA: analisis_glicemia ---
- id_glicemia (integer, NOT NULL)
- tipo_muestra (character varying, NOT NULL)
- horas_ayuno (smallint, NULL)
- glucosa_mg_dl (numeric, NOT NULL)
- glucosa_basal_mgdl (numeric, NULL)
- glucosa_60min_mgdl (numeric, NULL)
- glucosa_120min_mgdl (numeric, NULL)
- interpretacion (character varying, NULL)
- metodo_analisis (character varying, NULL)
- muestra_tipo (character varying, NULL)
- observaciones (text, NULL)
- id_responsable (integer, NOT NULL)
- fecha_analisis (timestamp with time zone, NOT NULL)

--- TABLA: analisis_grupo_sanguineo ---
- id_analisis_gs (integer, NOT NULL)
- reaccion_anti_a (character varying, NULL)
- reaccion_anti_b (character varying, NULL)
- reaccion_anti_ab (character varying, NULL)
- reaccion_anti_d (character varying, NULL)
- rh_debil_du (boolean, NULL)
- id_grupo_resultado (integer, NOT NULL)
- tecnica_utilizada (character varying, NULL)
- prueba_cruzada_mayor (character varying, NULL)
- prueba_cruzada_menor (character varying, NULL)
- observaciones (text, NULL)
- id_responsable (integer, NOT NULL)
- fecha_analisis (timestamp with time zone, NOT NULL)

--- TABLA: analisis_laboratorio ---
- id_analisis_lab (integer, NOT NULL)
- id_cliente (integer, NOT NULL)
- id_usuario_registro (integer, NOT NULL)
- id_parasitologia (integer, NULL)
- id_analisis_gs (integer, NULL)
- id_glicemia (integer, NULL)
- id_vih (integer, NULL)
- id_chagas (integer, NULL)
- observacion (text, NULL)
- fecha_registro (timestamp with time zone, NOT NULL)

--- TABLA: analisis_parasitologia ---
- id_parasitologia (integer, NOT NULL)
- consistencia_heces (character varying, NULL)
- color_heces (character varying, NULL)
- sangre_macroscopica (boolean, NULL)
- moco_macroscopico (boolean, NULL)
- leucocitos (character varying, NULL)
- eritrocitos (character varying, NULL)
- celulas_epiteliales (character varying, NULL)
- levaduras (boolean, NULL)
- parasito_encontrado (boolean, NOT NULL)
- nombre_parasito (character varying, NULL)
- estadio_parasito (character varying, NULL)
- cantidad_parasitos (character varying, NULL)
- metodo_directo (boolean, NULL)
- metodo_concentracion (boolean, NULL)
- metodo_kato_katz (boolean, NULL)
- tincion_utilizada (character varying, NULL)
- resultado_general (text, NOT NULL)
- observaciones (text, NULL)
- id_responsable (integer, NOT NULL)
- fecha_analisis (timestamp with time zone, NOT NULL)

--- TABLA: analisis_vih ---
- id_vih (integer, NOT NULL)
- prueba_tamizaje (character varying, NOT NULL)
- resultado_tamizaje (character varying, NOT NULL)
- indice_s_co (numeric, NULL)
- prueba_confirmatoria (character varying, NULL)
- resultado_confirmatorio (character varying, NULL)
- tipo_muestra (character varying, NULL)
- resultado_final (character varying, NOT NULL)
- control_positivo (boolean, NULL)
- control_negativo (boolean, NULL)
- consejeria_pretest (boolean, NULL)
- consejeria_postest (boolean, NULL)
- observaciones (text, NULL)
- id_responsable (integer, NOT NULL)
- fecha_analisis (timestamp with time zone, NOT NULL)

--- TABLA: clientes ---
- id_cliente (integer, NOT NULL)
- nombre (character varying, NOT NULL)
- apellido (character varying, NOT NULL)
- fecha_nacimiento (date, NOT NULL)
- ci (character varying, NOT NULL)
- domicilio (text, NOT NULL)
- nro_celular (character varying, NULL)
- fecha_registro (timestamp with time zone, NOT NULL)
- lugar_trabajo (character varying, NULL)
- foto (text, NULL)
- tipo_tramite (character varying, NULL)
- id_tipo_trabajo (integer, NULL)
- grupo_sanguineo (character varying, NULL)

--- TABLA: grupos_sanguineos ---
- id_grupo (integer, NOT NULL)
- nombre_grupo (character varying, NOT NULL)
- descripcion (text, NULL)

--- TABLA: historial_medico ---
- id_historial (integer, NOT NULL)
- id_analisis_lab (integer, NOT NULL)
- id_cliente (integer, NOT NULL)
- id_usuario (integer, NOT NULL)
- diagnostico (text, NOT NULL)
- tratamiento (text, NULL)
- fecha_consulta (timestamp with time zone, NOT NULL)
- observaciones (text, NULL)

--- TABLA: roles ---
- id_rol (integer, NOT NULL)
- nombre_rol (character varying, NOT NULL)
- descripcion (text, NULL)

--- TABLA: tipos_trabajo ---
- id_tipo_trabajo (integer, NOT NULL)
- descripcion (character varying, NOT NULL)

--- TABLA: usuarios ---
- id_usuario (integer, NOT NULL)
- nombre (character varying, NOT NULL)
- apellido (character varying, NOT NULL)
- ci (character varying, NOT NULL)
- id_rol (integer, NOT NULL)
- contrasena_hash (text, NOT NULL)
- fecha_creacion (timestamp with time zone, NOT NULL)
- activo (boolean, NOT NULL)
